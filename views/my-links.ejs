<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- SEO Meta Tags -->
    <title>내 링크 관리 - WPST Shop | URL 단축 링크 통계 및 관리</title>
    <meta name="description" content="WPST Shop에서 생성한 단축 링크들을 관리하고 클릭 통계를 확인하세요. 실시간 방문자 분석과 링크 성과를 한눈에 파악할 수 있습니다.">
    <meta name="robots" content="noindex, nofollow">
    <link rel="canonical" href="https://www.wpst.shop/my-links">
    
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>📋 내 링크들</h1>
            <p>내가 만든 단축 URL들을 관리하세요</p>
            <div class="nav-buttons">
                <a href="/" class="nav-btn">🔗 새 링크 만들기</a>
            </div>
        </header>

        <main>
            <% if (links.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-icon">🔗</div>
                    <h3>아직 만든 링크가 없습니다</h3>
                    <p>첫 번째 단축 URL을 만들어보세요!</p>
                    <a href="/" class="create-link-btn">링크 만들기</a>
                </div>
            <% } else { %>
                <div class="links-grid">
                    <% links.forEach(function(link) { %>
                        <div class="link-card">
                            <div class="link-header">
                                <div class="link-info">
                                    <h3>🔗 <%= link.shortCode %></h3>
                                    <div class="link-stats">
                                        <span class="visits">👆 <%= link.clicks %>명 방문</span>
                                        <span class="date">📅 <%= new Date(link.createdAt).toLocaleDateString('ko-KR') %></span>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="link-content">
                                <div class="url-section">
                                    <div class="url-display">
                                        <input type="text" value="<%= link.shortUrl %>" readonly class="url-input-readonly">
                                        <button onclick="copyUrl('<%= link.shortUrl %>')" class="copy-btn-small">복사</button>
                                    </div>
                                </div>
                                
                                <div class="original-url">
                                    <a href="<%= link.originalUrl %>" target="_blank" class="original-link">
                                        → <%= link.originalUrl.length > 60 ? link.originalUrl.substring(0, 60) + '...' : link.originalUrl %>
                                    </a>
                                </div>
                            </div>
                            
                            <div class="link-actions">
                                <a href="<%= link.shortUrl %>" target="_blank" class="action-btn visit-btn">🚀 방문하기</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
                
                <div class="summary">
                    <div class="summary-card">
                        <h3>📈 요약</h3>
                        <div class="summary-stats">
                            <div class="stat">
                                <span class="stat-number"><%= links.length %></span>
                                <span class="stat-label">총 링크</span>
                            </div>
                            <div class="stat">
                                <span class="stat-number"><%= links.reduce((sum, link) => sum + link.clicks, 0) %></span>
                                <span class="stat-label">총 클릭</span>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </main>

        <footer>
            <p>간단하고 빠른 URL 단축 서비스 | WPST Shop | <a href="https://www.wpst.shop" style="color: inherit;">www.wpst.shop</a></p>
        </footer>
    </div>

    <script>
        function copyUrl(url) {
            navigator.clipboard.writeText(url).then(function() {
                const btn = event.target;
                const originalText = btn.textContent;
                btn.textContent = '복사됨!';
                btn.style.background = '#27ae60';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                }, 2000);
            }).catch(function() {
                const input = document.createElement('input');
                input.value = url;
                document.body.appendChild(input);
                input.select();
                document.execCommand('copy');
                document.body.removeChild(input);
                
                const btn = event.target;
                const originalText = btn.textContent;
                btn.textContent = '복사됨!';
                btn.style.background = '#27ae60';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                }, 2000);
            });
        }
    </script>
</body>
</html>
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- SEO Meta Tags -->
    <title>WPST Shop - 무료 URL 단축 서비스 | 긴 링크를 짧게 만드는 최고의 도구</title>
    <meta name="description" content="WPST Shop의 무료 URL 단축 서비스로 긴 링크를 짧고 기억하기 쉬운 주소로 변환하세요. 클릭 통계, 방문자 분석 기능을 제공하는 한국 최고의 단축 URL 서비스입니다.">
    <meta name="keywords" content="URL 단축, 링크 단축, 단축 URL, 무료 URL 단축기, 링크 줄이기, 클릭 통계, 방문자 분석, WPST Shop, 한국 단축 서비스">
    <meta name="author" content="WPST Shop">
    <meta name="robots" content="index, follow">
    <meta name="language" content="ko">
    <meta name="revisit-after" content="1 days">
    <link rel="canonical" href="https://wpst.shop/">

    <!-- Open Graph Meta Tags -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="WPST Shop - 무료 URL 단축 서비스">
    <meta property="og:description" content="긴 링크를 짧고 기억하기 쉬운 주소로 변환하는 무료 URL 단축 서비스. 클릭 통계와 방문자 분석 기능 제공.">
    <meta property="og:url" content="https://wpst.shop/">
    <meta property="og:site_name" content="WPST Shop">
    <meta property="og:locale" content="ko_KR">

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="WPST Shop - 무료 URL 단축 서비스">
    <meta name="twitter:description" content="긴 링크를 짧고 기억하기 쉬운 주소로 변환하는 무료 URL 단축 서비스">
    <meta name="twitter:url" content="https://wpst.shop/">

    <link rel="stylesheet" href="/style.css">

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebApplication",
        "name": "WPST Shop URL 단축 서비스",
        "description": "무료 URL 단축 서비스로 긴 링크를 짧고 기억하기 쉬운 주소로 변환합니다. 클릭 통계와 방문자 분석 기능을 제공합니다.",
        "url": "https://wpst.shop",
        "applicationCategory": "UtilityApplication",
        "operatingSystem": "Any",
        "offers": {
            "@type": "Offer",
            "price": "0",
            "priceCurrency": "KRW"
        },
        "provider": {
            "@type": "Organization",
            "name": "WPST Shop",
            "url": "https://wpst.shop"
        },
        "featureList": [
            "무료 URL 단축",
            "클릭 통계 제공",
            "방문자 분석",
            "실시간 링크 관리"
        ]
    }
    </script>
</head>
<body>
    <div class="container">
        <header>
            <h1>🔗 WPST Shop</h1>
            <p>긴 URL을 짧고 간편하게! 무료 링크 단축 서비스</p>
        </header>

        <main>
            <div class="form-container">
                <form action="/shorten" method="POST" class="url-form">
                    <div class="input-group">
                        <input 
                            type="url" 
                            name="url" 
                            placeholder="링크를 추가하세요"
                            class="url-input"
                            required
                        >
                        <button type="submit" class="submit-btn">단축하기</button>
                    </div>
                </form>
            </div>

            <% if (error) { %>
                <div class="message error">
                    <span class="icon">❌</span>
                    <%= error %>
                </div>
            <% } %>

            <% if (shortUrl) { %>
                <div class="result-container">
                    <div class="message success">
                        <span class="icon">✅</span>
                        단축 URL이 생성되었습니다!
                    </div>
                    
                    <div class="result-box">
                        <label>단축된 URL:</label>
                        <div class="url-result">
                            <input 
                                type="text" 
                                value="<%= shortUrl %>" 
                                readonly 
                                class="result-input"
                                id="shortUrl"
                            >
                            <button onclick="copyToClipboard()" class="copy-btn">복사</button>
                        </div>
                        <small class="hint">위 링크를 클릭하면 원본 URL로 이동합니다.</small>
                    </div>
                </div>
            <% } %>

            <!-- 내 링크 목록 섹션 -->
            <% if (links && links.length > 0) { %>
                <div class="my-links-section">
                    <h2>📋 회원들이 만든 링크들</h2>
                    <div class="links-grid">
                        <% links.forEach(function(link) { %>
                            <div class="link-card-mini">
                                <div class="link-header-mini">
                                    <h4>🔗 <%= link.shortCode %></h4>
                                    <div class="link-stats-mini">
                                        <span class="visits">총방문자 👆 <%= link.clicks %>명</span>
                                        <span class="date">📅 <%= new Date(link.createdAt).toLocaleDateString('ko-KR') %></span>
                                    </div>
                                </div>
                                
                                <div class="link-content-mini">
                                    <div class="url-display-mini">
                                        <a href="<%= link.shortUrl %>" target="_blank" class="short-url-link">
                                            <%= link.shortUrl %>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    
                    <!-- 페이지네이션 -->
                    <% if (links && links.length > 0 && pagination.totalPages > 1) { %>
                        <div class="pagination">
                            <% if (pagination.hasPrev) { %>
                                <a href="/?page=<%= pagination.currentPage - 1 %>" class="pagination-btn">← 이전</a>
                            <% } %>
                            
                            <div class="pagination-info">
                                <span class="page-current"><%= pagination.currentPage %></span> / 
                                <span class="page-total"><%= pagination.totalPages %></span>
                                <small>(<%= pagination.totalLinks %>개 링크)</small>
                            </div>
                            
                            <% if (pagination.hasNext) { %>
                                <a href="/?page=<%= pagination.currentPage + 1 %>" class="pagination-btn">다음 →</a>
                            <% } %>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </main>

        <footer>
            <p>간단하고 빠른 URL 단축 서비스 | WPST Shop | <a href="https://wpst.shop" style="color: inherit;">wpst.shop</a></p>
        </footer>
    </div>

    <script>
        function copyToClipboard() {
            const urlInput = document.getElementById('shortUrl');
            urlInput.select();
            urlInput.setSelectionRange(0, 99999);
            
            try {
                document.execCommand('copy');
                
                const copyBtn = document.querySelector('.copy-btn');
                const originalText = copyBtn.textContent;
                copyBtn.textContent = '복사됨!';
                copyBtn.style.background = '#27ae60';
                
                setTimeout(() => {
                    copyBtn.textContent = originalText;
                    copyBtn.style.background = '';
                }, 2000);
            } catch (err) {
                alert('복사에 실패했습니다. 수동으로 복사해주세요.');
            }
        }

        function copyUrl(url) {
            navigator.clipboard.writeText(url).then(function() {
                const btn = event.target;
                const originalText = btn.textContent;
                btn.textContent = '복사됨!';
                btn.style.background = '#27ae60';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                }, 2000);
            }).catch(function() {
                const input = document.createElement('input');
                input.value = url;
                document.body.appendChild(input);
                input.select();
                document.execCommand('copy');
                document.body.removeChild(input);
                
                const btn = event.target;
                const originalText = btn.textContent;
                btn.textContent = '복사됨!';
                btn.style.background = '#27ae60';
                
                setTimeout(() => {
                    btn.textContent = originalText;
                    btn.style.background = '';
                }, 2000);
            });
        }

        document.querySelector('.url-form').addEventListener('submit', function(e) {
            const urlInput = document.querySelector('.url-input');
            const url = urlInput.value.trim();
            
            if (!url) {
                e.preventDefault();
                alert('URL을 입력해주세요.');
                return;
            }
            
            if (!url.startsWith('http://') && !url.startsWith('https://')) {
                e.preventDefault();
                alert('http:// 또는 https://로 시작하는 URL을 입력해주세요.');
                return;
            }
        });
    </script>
</body>
</html>